name: Update ShadowWhisperer Tracking List

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  update-rules:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Download ShadowWhisperer filter list
        run: |
          curl -fsSL \
            https://raw.githubusercontent.com/AdguardTeam/HostlistsRegistry/main/filters/general/filter_69_ShadowWhispererTrackingList/filter.txt \
            -o filter.txt

      - name: Extract domains from the filter list
        run: |
          grep -Eo '([a-zA-Z0-9.-]+\.[a-zA-Z]{2,})' filter.txt \
            | sort -u \
            > domain_list.txt || true

      - name: Generate domain_list.json for sing-box
        run: |
          jq -R . domain_list.txt | jq -s '{
            version: 3,
            rules: [
              { domain_suffix: . }
            ]
          }' > domain_list.json

      - name: Commit and push changes
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"

          # 关键修复：删除中间文件，避免 untracked 导致 commit 失败
          rm -f filter.txt

          git add domain_list.txt domain_list.json

          if [ -n "$(git status --porcelain)" ]; then
            git commit -m "Update ShadowWhisperer Tracking List ($(date -u +%Y-%m-%d))"
            git push
          else
            echo "No changes to commit."
          fi
